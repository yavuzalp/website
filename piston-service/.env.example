PORT=3000
PISTON_URL=http://piston:2000
ALLOWED_ORIGINS=https://algoarena.yavuzalp.com,http://localhost:5000

# Paste the FULL contents of a Firebase service-account JSON key as a single
# line (Project settings -> Service accounts -> Generate new private key).
# Used to verify player ID tokens server-side. Never commit the real value.
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"yavuzalpturkoglu",...}
