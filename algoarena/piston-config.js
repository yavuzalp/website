// ============================================================================
// ARENA API CONFIG — placeholder. Replace with your deployed piston-service
// Railway URL once it's up (see piston-service/README.md).
// ============================================================================
// Until this is replaced, Run/Submit buttons are disabled with an explanatory
// message instead of throwing errors — everything else in the app (lobby,
// rooms, presence) still works so you can test that part independently.
window.ALGOARENA_API_URL = "https://REPLACE_ME.up.railway.app";
